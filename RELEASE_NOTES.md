# NalaApps Macro v1.1.0

키보드·마우스·문자·시간 동작을 실제 매크로 제작 흐름에 맞게 보강하고 자동 회귀 검증을 추가한 안정화 버전입니다.

## 핵심 수정

- 동작하지 않던 마우스 클릭 흐름을 새 실행 엔진으로 교체
- 현재 위치와 고정 좌표 클릭 지원
- 왼쪽 클릭, 오른쪽 클릭, 더블클릭, 휠 위·아래 지원
- 횟수 지정 반복 클릭과 지정 시간 연속 클릭 지원
- `문자` 버튼에서 직접 키보드 입력이 가능한 다중행 입력창 제공
- 한글·영문·숫자·유니코드, Enter, Tab 실행 지원
- `동작` 버튼을 눌렀을 때 세부 동작 메뉴가 명확하게 열리도록 수정
- 키보드 A~Z, 0~9, F1~F24 및 조합키 지원
- Space 등 키를 지정 시간 동안 누르고 있기 지원
- 대기·키 유지·연속 클릭 시간을 1~86,400초 범위에서 직접 입력
- 파란색·빨간색 버튼 글자를 흰색으로 강제

## 신뢰성·안전성

- Windows `SendInput` 실패를 감지해 오류로 보고
- 실행 중단·취소·종료 시 눌린 키와 마우스 버튼 안전 해제
- `Ctrl+Alt+Delete` 입력 차단
- 반복 횟수·간격·시간·문자 길이 경계값 검증
- 손상되거나 허용 범위를 벗어난 저장 파일 차단
- 기존 v1 저장 파일을 불러올 수 있도록 호환 처리

## 자동 테스트와 품질 게이트

- 키 파싱, 조합키, 마우스 클릭, 반복·연속 클릭, 문자 입력, 시간, 취소 안전성, 직렬화, UI 이벤트 연결을 자동 검증
- Release 빌드에서 모든 경고를 오류로 처리
- 테스트 통과 후에만 self-contained 단일 EXE와 ZIP 생성
- ISO/IEC/IEEE 29119 테스트 프로세스 개념과 ISO/IEC 25010 제품 품질 특성을 참고한 테스트 계획·품질 평가·결과 보고서 포함

이 적용은 프로젝트 내부의 표준 정렬 및 품질 개선 활동이며 제3자 공식 인증을 의미하지 않습니다.

## 배포 파일

- `NalaApps-Macro-v1.1.0-Portable-win-x64.zip`
- `SHA256SUMS.txt`

압축을 해제한 뒤 `NalaApps.Macro.exe`를 실행하면 됩니다.

## 주의사항

- 관리자 권한 프로그램에 입력하려면 NalaApps Macro도 같은 권한 수준으로 실행해야 할 수 있습니다.
- 무한 반복이나 연속 클릭 전에 `Ctrl + Alt + F12` 긴급 중지 단축키가 등록되었는지 확인하십시오.
- 모니터 배치·해상도·배율이 변경되면 고정 좌표를 다시 확인하십시오.
