<Window x:Class="NalApps.Macro.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="470" Height="520"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        Opacity="0">
    <Border CornerRadius="18" Background="#F5F6F8" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="8" Opacity="0.22" />
        </Border.Effect>
        <Grid>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#F8FBFF" Offset="0" />
                    <GradientStop Color="#EEF4FF" Offset="1" />
                </LinearGradientBrush>
            </Grid.Background>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Width="92" Height="92" CornerRadius="28" Background="#3182F6" HorizontalAlignment="Center">
                    <TextBlock Text="N" Foreground="White" FontSize="52" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <TextBlock Text="날라앱스 매크로" FontSize="25" FontWeight="Bold" Foreground="#191F28" HorizontalAlignment="Center" Margin="0,24,0,0" />
                <TextBlock Text="반복 작업을 더 쉽고 빠르게" FontSize="13" Foreground="#8B95A1" HorizontalAlignment="Center" Margin="0,8,0,0" />
                <Border Width="170" Height="5" CornerRadius="3" Background="#DDE8F8" Margin="0,34,0,0">
                    <Border Width="70" HorizontalAlignment="Left" CornerRadius="3" Background="#3182F6" />
                </Border>
            </StackPanel>
            <TextBlock Text="NalaApps Utility" FontSize="10" Foreground="#A7B0BC" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,24" />
        </Grid>
    </Border>
</Window>
